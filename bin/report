#!/bin/bash

dir=$(dirname $0)

source "$HOME/.netqualrc"

target_file="$DB_DIR/report.txt"

(
	flock -w 10 9 || exit 1

	cat >>"$target_file"

	if [ ! -e "$target_file" ]; then
		echo "No target file"
	fi

	$dir/upload
) 9>>"$target_file"
